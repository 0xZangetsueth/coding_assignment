import React from 'react';
import './Token.css';

const Token = ({
  name,
  price,
  symbol,
  marketcap,
  volume,
  image,
  priceChange
}) => {
  return (
    <>
	 // {Token.name}
    </>
  );
};

export default Token;
